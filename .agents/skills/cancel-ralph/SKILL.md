---
name: cancel-ralph
description: "Use when the user explicitly invokes $cancel-ralph or asks to run that workflow. Cancel active Ralph Wiggum loop"
---

<!-- Generated by scripts/sync-agent-adapters.mjs; do not edit directly. -->

# Cancel Ralph

This is the Codex adapter for the canonical Claude capability at
`.claude/plugins/ralph-wiggum/commands/cancel-ralph.md`.

When this skill is selected:

1. Resolve the repository root with `git rev-parse --show-toplevel`.
2. Read `<repo>/.agents/CODEX_COMPATIBILITY.md` completely.
3. Read `<repo>/.claude/plugins/ralph-wiggum/commands/cancel-ralph.md` completely, including any referenced files needed for the task.
4. Follow the same workflow order, approval gates, safety rules, and verification requirements.
5. Treat text after `$cancel-ralph` as the canonical command's arguments when applicable.
6. Translate only platform surfaces; do not change canonical behavior or silently skip steps.
7. Do not edit the canonical source merely to make Codex execute it.

## Codex bridge

From the repository root, run the portable Ralph adapter's `cancel` action (`python3 .codex/hooks/ralph_adapter.py cancel` on Unix or `powershell -NoProfile -ExecutionPolicy Bypass -File .codex/hooks/run-python.ps1 .codex/hooks/ralph_adapter.py cancel` on Windows); do not delete state with a platform-specific shell command.
