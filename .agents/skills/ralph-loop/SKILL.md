---
name: ralph-loop
description: "Use when the user explicitly invokes $ralph-loop or asks to run that workflow. Start Ralph Wiggum loop in current session"
---

<!-- Generated by scripts/sync-agent-adapters.mjs; do not edit directly. -->

# Ralph Loop Command

This is the Codex adapter for the canonical Claude capability at
`.claude/plugins/ralph-wiggum/commands/ralph-loop.md`.

When this skill is selected:

1. Resolve the repository root with `git rev-parse --show-toplevel`.
2. Read `<repo>/.agents/CODEX_COMPATIBILITY.md` completely.
3. Read `<repo>/.claude/plugins/ralph-wiggum/commands/ralph-loop.md` completely, including any referenced files needed for the task.
4. Follow the same workflow order, approval gates, safety rules, and verification requirements.
5. Treat text after `$ralph-loop` as the canonical command's arguments when applicable.
6. Translate only platform surfaces; do not change canonical behavior or silently skip steps.
7. Do not edit the canonical source merely to make Codex execute it.

## Codex bridge

Do not execute the Bash-only Claude setup script. From the repository root, run `python3 .codex/hooks/ralph_adapter.py start ...` on Unix, or `powershell -NoProfile -ExecutionPolicy Bypass -File .codex/hooks/run-python.ps1 .codex/hooks/ralph_adapter.py start ...` on Windows, followed by the user's prompt and options. Then work on that prompt. The adapter writes the same `.claude/ralph-loop.local.md` state used by Claude.
