---
name: explanatory-output-style
description: "Use when the user requests explanatory output style with concise educational implementation insights."
---

<!-- Generated by scripts/sync-agent-adapters.mjs; do not edit directly. -->

# Explanatory Output Style Plugin

This is the Codex adapter for the canonical Claude capability at
`.claude/skills/explanatory-output-style/README.md`.

When this skill is selected:

1. Resolve the repository root with `git rev-parse --show-toplevel`.
2. Read `<repo>/.agents/CODEX_COMPATIBILITY.md` completely.
3. Read `<repo>/.claude/skills/explanatory-output-style/README.md` completely, including any referenced files needed for the task.
4. Follow the same workflow order, approval gates, safety rules, and verification requirements.
5. Treat text after `$explanatory-output-style` as the canonical command's arguments when applicable.
6. Translate only platform surfaces; do not change canonical behavior or silently skip steps.
7. Do not edit the canonical source merely to make Codex execute it.

## Codex bridge

For the current task, read and apply the canonical SessionStart context in `.claude/skills/explanatory-output-style/hooks-handlers/session-start.sh`. Set `CODEX_OUTPUT_STYLE=explanatory` before starting Codex to select it for a new session.
