---
name: codebase-structure
description: "Use BEFORE creating or editing any component, page, function, hook, util, service, or file. Forces reuse before creation, correct placement, single responsibility (no GOD files), and consistent naming so the codebase stays clean and maintainable. Triggers on \"add/create a component\", \"new page\", \"write a function\", \"add an endpoint\", any new file, or any file growing large. Pattern-agnostic — not DDD, not Atomic — just good file/code hygiene."
---

<!-- Generated by scripts/sync-agent-adapters.mjs; do not edit directly. -->

# Codebase Structure

This is the Codex adapter for the canonical Claude capability at
`.claude/skills/codebase-structure/SKILL.md`.

When this skill is selected:

1. Resolve the repository root with `git rev-parse --show-toplevel`.
2. Read `<repo>/.agents/CODEX_COMPATIBILITY.md` completely.
3. Read `<repo>/.claude/skills/codebase-structure/SKILL.md` completely, including any referenced files needed for the task.
4. Follow the same workflow order, approval gates, safety rules, and verification requirements.
5. Treat text after `$codebase-structure` as the canonical command's arguments when applicable.
6. Translate only platform surfaces; do not change canonical behavior or silently skip steps.
7. Do not edit the canonical source merely to make Codex execute it.
