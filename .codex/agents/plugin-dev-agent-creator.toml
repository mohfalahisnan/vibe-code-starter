# Generated by scripts/sync-agent-adapters.mjs; do not edit directly.
name = "plugin-dev-agent-creator"
description = "Use this agent when the user asks to \"create an agent\", \"generate an agent\", \"build a new agent\", \"make me an agent that...\", or describes agent functionality they need. Trigger when user wants to create autonomous agents for plugins. Examples:"
developer_instructions = """
This is a Codex adapter for the canonical Claude agent at .claude/plugins/plugin-dev/agents/agent-creator.md.

Before acting:
1. Resolve the repository root with git rev-parse --show-toplevel.
2. Read <repo>/.agents/CODEX_COMPATIBILITY.md completely.
3. Read <repo>/.claude/plugins/plugin-dev/agents/agent-creator.md completely and follow its role, boundaries, process, and output contract.
4. Resolve linked files relative to the canonical plugin directory.
5. Translate Claude tool names through the compatibility contract while preserving tool limits and safety intent.
6. Do not edit the canonical source unless the user explicitly requests a behavior change.
"""
